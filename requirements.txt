# Core UI
flet>=0.86,<0.87

# Crypto
cryptography>=42.0
argon2-cffi>=23.1

# --- Optional features (install only what you need) ---
# Google Drive backup:
google-auth>=2.27
google-auth-oauthlib>=1.2
google-api-python-client>=2.120

# Document scanning / image processing (Phase 1):
opencv-python-headless>=4.9
numpy>=1.26
Pillow>=10.0
img2pdf>=0.5

# On-device OCR (Phase 2, English):
# rapidocr-onnxruntime>=1.3

# PDF page tools (Phase 3):
# pypdf>=4.0
